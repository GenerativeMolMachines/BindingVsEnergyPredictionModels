version: "3.8"

services:
  app:
    build:
      context: .
    restart: always
    container_name: aptamer_gen_model
    command: uvicorn main:app --host 0.0.0.0 --port 4395 --workers 2
    ports:
      - "4395:4395"
    networks:
      - seq_backend

networks:
  seq_backend: